version: '3.8'

services:
  utm-qr-app:
    image: nginx:alpine
    container_name: utm-qr-app
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
